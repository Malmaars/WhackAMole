using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
public class Rectangle : IClickable
{
    public Vector2 position { get; set; }

    public float height, width;
    public virtual void OnClick() { }

    public bool CheckForClick()
    {
        List<Vector2> touches = InputHandler.CheckForInputs();

        foreach (Vector2 touch in touches)
        {
            if (Mathf.Abs(touch.x - position.x) <= width &&
                Mathf.Abs(touch.y - position.y) <= height)
            {
                return true;
            }
        }

        return false;
    }
}
*/
