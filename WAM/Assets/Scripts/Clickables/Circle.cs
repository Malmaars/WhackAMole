using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
public class Circle : IClickable
{
    public Vector2 position { get; set; }

    public float radius;
    public virtual void OnClick() { }

    public bool CheckForClick()
    {
        List<Vector2> touches = InputHandler.CheckForInputs();

        foreach (Vector2 touch in touches)
        {
            if (Vector2.Distance(touch, position) < radius)
            {
                return true;
            }
        }

        return false;
    }
}*/
