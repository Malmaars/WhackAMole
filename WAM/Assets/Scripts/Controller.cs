using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//The Controller will manage all player inputs. Every button clicked will trigger the Controller, which in turn
//returns the proper data to the model, which handles all functionality

//Furthermore, the controller passes along the correct visuals to the viewmanager, which updates
//The visuals depending on the data it receives
public class Controller : MonoBehaviour
{
    //public IView ViewManager;
    //IModel Model;

    //private void Start()
    //{
    //    Model.OnStart();
    //}

    //private void Update()
    //{
    //    Model.OnUpdate();
    //}
}
